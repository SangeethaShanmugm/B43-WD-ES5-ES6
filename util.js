//named export
const printDetails4 = ({ name, score, batch }) =>
  `${name} has got ${score} and she belongs to ${batch}`;

const double = (n) => n * 2;

const Msg = "This is awesome and super Cool🥳🥳";

// export default Msg;
export default printDetails4;
// export { printDetails4, double, Msg }; //named export

// import and export
// 2 types
//1. named -import and export  - preferred
//2. default -import and export


<div></div>